# Derivados de Office
~$*
*.pdf

# Retirado como fuente de la app (docs/guia-terminologia.html lo sustituye);
# se queda en el disco del docente sin versionarse, por si le vale para otra cosa.
docs/Guia_terminologia_rubricas_LOMLOE.docx

# Python
__pycache__/
*.pyc
.venv/

# Node (cuando empiece la aplicación)
node_modules/

# Sistema
Thumbs.db
desktop.ini
.DS_Store

# Ficheros de bloqueo de LibreOffice/Office mientras un .docx está abierto
.~lock.*#

# Copias completas del repositorio hechas por el agente: nunca se buscan
.claude/worktrees/
